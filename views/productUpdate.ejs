<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
    />
    <title>Product</title>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/allproducts">All Products</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/allcategory"
                >Category-Master</a
              >
            </li>
            <li class="nav-item">
              <a class="nav-link active" href="/productMaster">Product-Master</a>
            </li>
            </ul>
        </div>
      </div>
    </nav><br>

    <form id="categoryForm" action="http://localhost:5000/createProduct" method="post">
      <center>

        <label for="productName"><b>Add Product :</b></label>
            <input type="text" name="name"/>      &nbsp&nbsp&nbsp&nbsp&nbsp&nbsp

        <label for="category"><b>Category Name:</b></label>
            <select id="category" name="category" required>
             <% for (let i = 0; i < result2.length; i++) { %>
                 <option value="<%= result2[i].CategoryId %>">
                     <%= result2[i].CategoryName %>
                 </option>
            <% } %>
            </select>
            
            &nbsp&nbsp&nbsp<button type="submit" id=".btn-primary " class="btn btn-primary btn-sm"> Add Product</button>
      </center>
    </form>

        <br>
    <table class="table table-striped table-hover" border="5px">
      <thead class="thead-light">
        <tr class="text-light bg-dark">
          <th scope="col">Sr.no</th>
          <th scope="col">ProductID</th>
          <th scope="col">ProductName</th>
          <th scope="col">CategoryID</th>
          <th scope="col">CategoryName</th>
          <th scope="col">Action</th>
        </tr>
      </thead>
      <tbody>
        <% for (let i = 0; i < data.length; i++) { %>

        <tr>
          <th scope="row"><%=i+1%></th>
          <td><%= data[i].ProductId %></td>
          <td><%= data[i].ProductName %></td>
          <td><%= data[i].CategoryId %></td>
          <td><%= data[i].CategoryName %></td>
          <td>
            <button class="btn btn-danger btn-sm"><a href="/delete/<%= data[i].ProductId %>" class="text-white "style="text-decoration:none" >Delete</a></button>&nbsp&nbsp&nbsp
            <button class="btn btn-success btn-sm"><a href="/pupdate/<%= data[i].ProductId %>" class="text-white "style="text-decoration:none" >Update</a></button>  
          </td>
        </tr>
        <% } %>
      </tbody>
    </table>
  </body>
</html>
